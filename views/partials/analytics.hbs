{{#each assessments}}
<section class="ui segment">
<div class="ui two small statistics">
    <div class="ui statistic">
        <div class="value">
            <i class="user icon"> </i>
        </div>
        <div class="label">
            <h2 class="ui header">{{member}} </h2>
        </div>
    </div>
    <div class="ui statistic">
        <div class="value">
            <i class="heartbeat icon"> </i> <!-- ${_bmi}-->
        </div>
        <div class="label">
            <h2 class="ui header"> BMI   </h2>
        </div>
    </div>
</div>
</section>

{{/each}}